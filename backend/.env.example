# Database
DATABASE_URL=sqlite:///./db/finance.db

# CORS
FRONTEND_URL=http://localhost:3000

# Market Data (optional — falls back to Yahoo Finance if not set)
FINNHUB_API_KEY=

# LLM Sentiment (optional — enables AI sentiment layer in recommendations)
OPENAI_API_KEY=

# Token Encryption for brokerage credentials
# Generate: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=
